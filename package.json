{
  "name": "hydrate",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm run -s build && npm run -s serve",
    "build": "npm run -s build-ts && npm run -s build-html",
    "build-ts": "tsc && ln -f -s ../../node_modules dist/client/",
    "build-html": "cp src/client/*.html dist/client/ && mkdir -p dist/routing/ && cp src/client/routing/*.html dist/client/routing/",
    "serve": "node dist/server.js",
    "clean": "rm -rf dist/"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1",
    "lit": "^2.2.8",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^18.6.4",
    "typescript": "^4.7.4"
  }
}
